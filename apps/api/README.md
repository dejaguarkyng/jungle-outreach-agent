# API orchestrator

The backend is deployed with Next.js route handlers under `app/api`. Stable
orchestration, Jungle Grid, artifact, persistence, and ZeptoMail modules are
exported through `apps/api/src/index.ts`.
