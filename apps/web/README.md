# Web application

The Next.js web application currently runs from the repository root (`app/`,
`components/`, and `src/`). This directory marks the web ownership boundary for
the public monorepo layout. A future package split can move those folders here
without changing API contracts.
